---
name: insight-shorts
description: 자료화면(슬라이드·지도·차트·뉴스헤드라인) 위주의 해설·강의 영상 하나로 9:16 세로 쇼츠 완성본 MP4 와 썸네일을 만든다. 전사 → 3안 기획 → 무음 제거 → 컷 글리치 안정화 → 순간 자료 확대 상단 밴드 → 순검정 완전 비간섭 한글 자막 하드번인 → 120pt 훅 타이틀 → 썸네일까지 자동. 설교 쇼츠(shorts-autopilot)와 달리 인물을 따라가지 않고 자료를 보여준다. "시사 쇼츠" / "정세 쇼츠" / "인사이트 쇼츠" / "강의 영상 쇼츠" / "자료화면 쇼츠" 로 발동한다.
---

# 🌏 insight-shorts — 자료화면 해설 영상 쇼츠 자동화

> **대상**: 국제정세·시사·강의처럼 **화면에 자료가 있고 그것을 설명하는** 영상.
> **핵심 차이**: shorts-autopilot 은 인물 한 명을 따라가는 파이프라인이다. 여기서는
> 인물이 아니라 **말하는 그 순간의 자료**를 화면에 채우는 것이 전부다.
> **자매 스킬**: 설교·인물 중심이면 `shorts-autopilot`, 찬양이면 `worship-shorts`.

---

## 🚨 금기 사항

1. **자료를 9:16 으로 잘라 화면을 꽉 채우지 마라 ❌**
   슬라이드·지도·차트는 가로로 넓다. 세로로 자르면 핵심이 잘려 나간다.
   기본 레이아웃은 **`band`** — 자료를 통째로 상단 밴드에 얹고 배경은 순검정(`bg: black`)이다.
   `fill`(9:16 전체화면)은 얼굴이 크게 잡히는 토킹헤드나 영상 클립에만 쓴다.
2. **자막이 메인 자료를 조금이라도 가리게 두지 마라 ❌**
   ★**기본 레이아웃은 완전 비간섭(`non-overlapping`)** (오너 지시 2026-09-07·08).
   자료 하단선(Y=1280) 아래 **순검정 여백 영역(`pos_y=1420`, `anchor=5`)**으로 자막을 내려
   메인 자료화면·인물·도표를 100% 가리지 않는다.
3. **자동 판정만 믿고 넘어가지 마라 ❌**
   "무엇을 확대할지"는 말의 내용에 달렸고 기계가 모른다. `scene_crop.py` 의 자동 판정은
   출발점일 뿐이다. **격자 프레임을 눈으로 보고 `plan.json` 의 `focus` 로 지정**해야 한다.
4. **`--stage b` 전에 `check_chunks.py` 를 건너뛰지 마라 ❌** (`✅ 일치` 없이 렌더 금지)
5. **자막에 그림자·외곽선을 넣지 마라 ❌** (오너 지시 2026-09-06)
   `BorderStyle=3` 불투명 **검은 박스**에 흰 글자. `Shadow=0`. 게이트 G-S 가 잡는다.
6. **영문 자막을 섞지 마라 ❌** — 한글 한 줄(길면 두 줄)만.
7. **자막 번인된 완성본에서 썸네일 프레임을 뽑지 마라 ❌**
   반드시 클린 마스터(`_work/{slug}_master.mov`)에서 `extract_frames_insight.py` 로 뽑는다.
8. **Whisper 를 컨텍스트 이월(기본값)로 장시간 돌리지 마라 ❌**
   2026-09-06 실측: 63분 강의에서 27분 지점부터 같은 문장("탄도미사일 발사 과정입니다")을
   19분간 반복하는 환각 루프에 빠졌다. 긴 원본 전사는 반드시 **`-mc 0`**(컨텍스트 끄기)로.
9. **맥락 없는 0.3~1.5초 이상 컷(글리치)을 방치하지 마라 ❌**
   순간 플래시 컷, 암전(순검정 프레임), 엉뚱한 이모지·그래픽, 스튜디오 잔여 컷 등은
   굳이 화면을 바꿀 필요가 없으므로 이전의 자연스러운 자료화면을 연장·유지하여
   클린 마스터(`clean_master.mov`)로 정리한 뒤 렌더링한다 (오디오 스트림 100% 무손실 유지).
10. **요약·축약 대본으로 자막을 때우지 마라 ❌**
    반드시 영상 오디오에서 Whisper large-v3로 실제 발화를 재전사하여 화자의 말소리와
    100% 일치(Verbatim)하도록 작성하고, STT 전문 용어 오인식을 철저히 교정한다.

---

## 🛠️ 오픈소스 기술 스택 & 단계별 프로그램 역할

본 파이프라인은 상용 솔루션이나 특정 유료 서비스에 종속되지 않는 **순수 오픈소스 생태계**로 동작합니다.

| 단계 | 공정 | 오픈소스 도구 / 모델 | 세부 역할 및 기술적 이유 |
|---|---|---|---|
| **1** | **DTW 전사** | **`Whisper.cpp`** (`whisper-cli`)<br>+ `ggml-large-v3.bin` | C/C++ 네이티브 Metal/CUDA 가속 고속 추론. 음성을 단어(words) 단위 센티초 정밀 타임스탬프(`t_dtw`)로 전사하여 컷 편집 기준선 및 자막 동기화 원천 데이터 생성 |
| **2** | **무음/호흡 컷** | **`FFmpeg`** (`silencedetect` 필터)<br>+ `Python` | 발화 사이의 불필요한 공백(-30dB~-26dB)을 자동 탐지하고 0.15s 안전 패딩을 두어 자연스럽게 호흡을 단축, 영상의 정보 밀도와 몰입감을 극대화 |
| **3** | **자료화면 컷 감지 & 와이드 밴드 레이아웃** | **`OpenCV`** (`cv2.VideoCapture`, `cv2.absdiff`)<br>+ `Python` (`scene_crop.py`) | 프레임 차분 기반 장면 전환(Scene Change) 감지. 16:9 자료화면을 9:16 프레임에 맞춰 좌우 15% 크롭하고 하단선(Y=1280) 기준 와이드 밴드 배치 좌표 자동 산출 |
| **4** | **이상 컷(글리치) 안정화** | **`FFmpeg`** (`trim`, `setpts`, `concat`, `tpad`)<br>+ `Python` | 원본 영상 속 0.3~1.5초 맥락 없는 AI 플래시/암전/이모지 컷을 식별하고 이전 정상 프레임을 동결 연장(`Hold Previous Frame`)하는 클린 마스터 생성 (`-c:a copy`로 오디오 100% 무손실 보존) |
| **5** | **비간섭 자막 & 상단 훅 타이틀** | **`Python`** + **`ASS`** (Advanced SubStation Alpha)<br>(`make_ass_box.py`) | 상단 120pt 2단 타이틀(`title_y=360`, 둘째 줄 골드 강조)과 자료 하단선 아래 순검정 영역(`pos_y=1420`, `an5`) 완전 비간섭 검은 박스(`BorderStyle=3`, 그림자 0) 자막 스트림 생성 |
| **6** | **자막 원문 일치 기계감사** | **`Python`** (`check_chunks.py`) | 자막 청크와 Whisper 전사 단어 목록을 토큰 스트림 단위로 전수 대조하여 임의 요약·누락을 원천 차단하는 100% 일치 하드 게이트 검사 (Gate 4) |
| **7** | **9:16 비디오 합성 & 하드번인** | **`FFmpeg`** + **`libass`** 라이브러리<br>(`render_insight.py`) | `filter_complex` 기반 단일 패스 렌더링. 순검정 배경, 테두리(Border)가 포함된 와이드 밴드 자료화면, 비간섭 ASS 자막 및 상단 타이틀을 고화질로 일괄 합성 (OS별 하드웨어 가속 지원) |
| **8** | **포토제닉 썸네일 & 안전영역 가이드** | **`Pillow`** (PIL) + **`FFmpeg`**<br>(`generate_thumbnail.py`) | 자막이 없는 클린 마스터에서 대표 프레임을 추출하고, 유튜브 쇼츠(9:16) 및 인스타그램 릴스(3:4) / 피드(4:5) 격자 안전영역 2단 훅 타이포그래피 카드 3종 자동 생성 |
| **9** | **완성본 재전사 교차검증** | **`Whisper.cpp`** + `difflib.SequenceMatcher`<br>+ **`NumPy`** (`check_insight.py`) | 최종 렌더링된 MP4 오디오를 재전사하여 대본과의 유사도(≥95.0%) 평가 및 17개 하드 게이트(블록 간격, 표시 시간, 레이아웃 비간섭, 폰트 규격 등) 전수 자동 판정 (Gate 7) |

### 🛠️ 실행 환경 및 요구사항

| 구성 | 권장 경로 / 환경변수 | 비고 |
|---|---|---|
| Python | Python 3.10+ (가상환경 `.venv`) | `opencv-python`, `numpy`, `pillow` |
| 파이프라인 스크립트 | `scripts/` (본 저장소 내장) | `run_insight.py`, `scene_crop.py` 등 |
| FFmpeg | `FFMPEG_PATH` 또는 시스템 `PATH` (`which ffmpeg`) | macOS: `brew install ffmpeg` |
| Whisper CLI | `WHISPER_BIN` 또는 `which whisper-cli` | `whisper.cpp` 빌드 바이너리 |
| Whisper 모델 | `WHISPER_MODEL` (기본 `ggml-large-v3.bin`) | 고품질 한국어 전사용 |
| 산출 위치 | `{작업폴더}/{YYYY-MM-DD}_{주제}/v4/out/` | 자유 지정 |

스크립트 구성:
- `run_insight.py`: 전체 오케스트레이터 (Stage A → Scene → Stage B)
- `scene_crop.py`: 컷 감지 및 밴드/자료화면 크롭 레이아웃 계산
- `make_ass_box.py`: 순검정 비간섭 자막(120pt 대형 훅 타이틀 연동) ASS 생성
- `render_insight.py`: FFmpeg 비디오 필터 합성 및 렌더링
- `extract_frames_insight.py`: 썸네일용 클린 프레임 추출
- `check_insight.py`: 레이아웃 및 ASS 스타일 검증 게이트
- `check_chunks.py`: 자막 텍스트와 Whisper 원문 스트림 100% 일치 기계 검증
- `generate_thumbnail.py`: 유튜브(9:16) 및 인스타그램(3:4/4:5) 안전영역 썸네일 카드 생성

---

## 🔄 워크플로우

### Phase 0 — 원본 전사 (기획 재료)

```bash
ffmpeg -y -i "{원본}" -vn -ac 1 -ar 16000 _work/full.wav
whisper-cli -m ggml-large-v3.bin -l ko -osrt -otxt --beam-size 5 --temperature 0 \
  -mc 0 -f _work/full.wav -of _work/full     # ★-mc 0 필수 (금기 8)
```
전사문 전체를 그대로 읽지 말고 **30초 단위 압축본**으로 접어서 읽는다(63분 → 약 127줄).

> 🛑 **Gate 0**: 같은 문장이 3회 이상 연속 반복되는 구간이 없는가? 있으면 환각 루프다 —
> 그 구간은 기획에 쓰지 말고, 필요하면 해당 구간만 다시 전사한다.

### Phase 1 — 관점 3안 기획

해설 영상 쇼츠는 설교의 OREO 와 다르다. **"주장 → 근거(자료) → 그래서 뭐"** 구조다.

- **H (Hook)**: 판을 뒤집는 한 문장 또는 숫자 (3~5초). 자료 화면이 같이 뜨면 강하다.
- **E (Evidence)**: 화면의 자료로 뒷받침. **이 구간이 곧 `focus` 확대 대상**이다.
- **S (So what)**: 시청자의 현실로 착지. 관점·질문·행동으로 닫는다.

⚠ 자료를 나열하고 끝내지 마라. "무슨 일이 있다"로 끝나면 뉴스 클립일 뿐이다.
**"그래서 우리는 무엇을 보아야 하는가"** 가 반드시 마지막에 온다.

플랜(`plans/plan_{ID}.json`):
```json
{
  "id": "A",
  "title": "한국 미사일 방어망의 진짜 문제",
  "slug": "A_한국_미사일_방어망의_진짜_문제",
  "segments": [[1130.0, 1165.0], [1240.5, 1268.0]],
  "preserve": [[1150.2, 1152.8]],
  "focus": [
    {"t0": 0.0,  "t1": 8.5,  "mode": "band", "rect": [305, 175, 345, 460], "note": "KAMD 카드"},
    {"t0": 8.5,  "t1": 19.5, "mode": "band", "rect": [880, 175, 390, 460], "note": "KMPR 카드"},
    {"t0": 19.5, "t1": 34.2, "mode": "band", "note": "전체 슬라이드"}
  ],
  "style": {"fs": 96, "pos_y": 1240, "band_y": 190, "band_h": 880},
  "script": "완성본에서 실제로 들릴 한국어 대본 전문"
}
```
- `focus` 의 `t0/t1` 은 **무음 제거가 끝난 마스터 타임라인** 기준이다. 그래서 Phase 2 이후에 쓴다.
- `rect` 는 **원본 프레임 좌표** `[x, y, w, h]`. 비율은 아무래도 좋다(band 가 알아서 맞춘다).
- `mode` 생략 시 `band`. 옛 이름 `zoom`·`fit` 도 band 로 읽는다.

> 🛑 **Gate 1**: ①맥락 없는 5~8초 조각이 아닌가 ②자료 나열로 끝나지 않고 관점으로 닫았는가
> ③`slug` 가 `{ID}_{한글제목}` 인가 ④웃음·감탄 등 살릴 리액션을 `preserve` 에 넣었는가

### Phase 2 — Stage A (구간 추출 → 무음 제거 → DTW 전사 → 자동 레이아웃)

```bash
.venv-cv/bin/python3 scripts/shorts_insight/run_insight.py \
  --src "{원본}" --plan plans/plan_A.json --outdir "{결과폴더}/v2/out" --stage a
```
산출: `_work/{slug}_master.mov` · `_work/{slug}_master.words.json` · `_work/{slug}_scene.json`

> 🛑 **Gate 2**: `t_dtw 적용: NN/NN` 이 90% 이상인가 · 마스터 길이가 기획 의도와 맞는가

### Phase 3 — ★focus 지정 (이 스킬의 핵심 · 사람이 하는 일)

마스터에서 격자 프레임을 뽑아 **눈으로 보고** 확대할 영역을 좌표로 읽는다:
```bash
for t in 2 9 16 26; do
  ffmpeg -y -ss $t -i _work/{slug}_master.mov -frames:v 1 \
    -vf "drawgrid=w=128:h=90:t=1:c=yellow@0.8" _work/g_$t.jpg; done
```
격자는 가로 128px·세로 90px 눈금이다(1280x720 기준 10x8칸). 카드·헤드라인·지도의
경계를 눈금으로 읽어 `rect` 를 적는다. ★**프레임을 타일로 축소해서 좌표를 읽지 마라.**
축소본에서 눈대중으로 읽으면 반드시 어긋난다(2026-09-06 실측: 헤드라인 4건을 y 10~410 으로
읽었는데 실제는 20~710 이었고, 확대했더니 3.5건만 들어왔다). 좌표를 확정할 프레임은
**원본 크기 그대로** 한 장씩 열어 본다.

**화면 전환 시점부터 찾아라.** `focus` 구간이 실제 전환과 어긋나면 엉뚱한 자리를 확대한다
(2026-09-06 실측: 슬라이드인 줄 알고 잡은 5~12.6초 구간이 실제로는 강사 클로즈업이었다).
`scene_crop` 이 로그에 찍은 컷 경계를 먼저 보고, 미심쩍으면 그 부근 프레임을 2~3초 간격으로
뽑아 확인한 뒤 구간을 나눈다. **말의 내용과 시각을 맞추는 것**이 목적이다:
그 문장이 KAMD 를 설명하면 KAMD 카드를, 숫자를 말하면 그 숫자가 있는 표를 확대한다.

`focus` 를 `plan.json` 에 적고 레이아웃만 다시 계산한다(전사·무음제거를 다시 하지 않는다):
```bash
.venv-cv/bin/python3 scripts/shorts_insight/run_insight.py \
  --src x --plan plans/plan_A.json --outdir "{결과폴더}/v2/out" --stage scene
```

> ⚠ **자동 판정을 그대로 믿으면 안 되는 두 경우** (2026-09-06 실측)
> - **뉴스 화면 속 인물**: 4분할 뉴스 클립의 김정은 얼굴을 YuNet 이 잡아 `fill`(토킹헤드)로
>   판정했다. 실제로는 자료 화면이라 `band` 로 덮어써야 한다.
> - **무대 전경**: 강사가 화면 우측 끝에 서 있는데 청중 쪽을 잡아 화면 왼쪽을 크롭했다.
>   전경 샷은 `band` 로 전체를 보여주거나, 강사 좌표를 직접 넣어 `fill` 로 지정한다.

> 🛑 **Gate 3**: 로그의 샷 목록에서 ①말하는 내용과 확대 영역이 맞는가 ②업스케일이 2.8배를
> 넘지 않는가 ③`rect` 가 자료를 잘라먹지 않는가(경계에 여유 20~30px 를 준다)

### Phase 4 — 자막 청크 & 기계 검증 (Hard Gate)

`_work/{slug}_master.words.json` 의 글자 스트림을 **10~14자 호흡**으로 끊는다.
```json
[{"stt":"그리고중층은청궁2","ko":"중층은 천궁 2"},
 {"stt":"요격할수있는확률이낮다고표현합니다.","ko":"요격 확률이 낮다고 말합니다","hl":true}]
```
- `ko`: 화면에 뜰 한글. **영문 없음.** 96pt 기준 한 줄 10자를 넘으면 자동 두 줄이 된다.
- `stt`: STT 원문(오인식 포함). 정렬은 이 값 기준이라 `ko` 를 자유롭게 교정할 수 있다.
- `hl: true`: 골드 강조. 편당 2~4개만.

```bash
.venv-cv/bin/python3 scripts/shorts_v2/check_chunks.py \
  --words _work/{slug}_master.words.json --chunks _work/chunks_{ID}.json
```
> 🛑 **Gate 4 (우회 불가)**: `✅ 일치` 가 뜨지 않으면 렌더로 넘어가지 않는다.

### Phase 5 — Stage B (자막 + 렌더 + 유사도 검증)

```bash
.venv-cv/bin/python3 scripts/shorts_insight/run_insight.py \
  --src "{원본}" --plan plans/plan_A.json --outdir "{결과폴더}/v2/out" \
  --stage b --chunks "{결과폴더}/v2/out/_work/chunks_A.json"
```
> 🛑 **Gate 5**: 완성본 길이가 마스터와 ±0.15초 이내인가 · `{slug}_verify.md` 유사도 95% 이상인가

### Phase 6 — 썸네일

클린 마스터에서 프레임을 뽑고(자막 번인본 금지), 카드로 만든다:
```bash
.venv-cv/bin/python3 scripts/shorts_insight/extract_frames_insight.py \
  --master _work/{slug}_master.mov --scene _work/{slug}_scene.json \
  --times 4 12 25 --outdir _work/frames --prefix {ID}

.venv-cv/bin/python3 scripts/shorts_v2/generate_thumbnail.py --mode both \
  --frame _work/frames/{ID}_004.00.jpg --badge "{카테고리 뱃지}" \
  --l1 "{1행}" --l2 "{2행 · 강조}" --sub "{티저}" \
  --out-yt "{outdir}/{slug}_thumb_youtube.jpg" \
  --out-insta "{outdir}/{slug}_thumb_instagram.jpg" \
  --out-guide "{outdir}/{slug}_thumb_instagram_guide.jpg"
```
★**썸네일용 프레임은 `--band-y` 를 크게 줘서 자료를 화면 아래로 내린다**(예: `--band-y 800
--band-h 900`). 본편과 같은 배치(자료가 상단)로 뽑으면 썸네일 문구가 자료 위에 겹쳐
둘 다 안 읽힌다(2026-09-06 실측: A안 헤드라인 배경에 문구가 묻혔다).

썸네일 문구는 **자료의 숫자·고유명사**를 쓴다("2년 안에", "GDP 23%"). 본문 자막과 같은
문장을 반복하지 않는다.

### Phase 7 — 게이트 & 인계

```bash
.venv-cv/bin/python3 scripts/shorts_insight/check_insight.py \
  --outdir "{결과폴더}/v2/out" --slug "{slug}"
```
`✅ 통과` 를 확인하고 `00_읽어보세요.md`(3안 요약·업로드 문구·썸네일 안내)를 쓴 뒤 폴더를 연다.

---

## 🎨 정본 스펙

### 레이아웃 프리셋

★**기본은 `non-overlapping` (완전 비간섭 레이아웃, 2026-09-08 정본)**
배경은 순검정, 상단 120pt 대형 2단 제목, 중앙 와이드 자료화면(좌우 15% 크롭, Y=1280 하단선),
자막은 자료 아래 순검정 영역(`pos_y=1420`)에 배치하여 메인 자료를 100% 가리지 않는다:

```json
"style": {"fs": 96, "pos_y": 1420, "anchor": 5, "max_chars": 10,
          "band_y": 480, "band_h": 800, "bg": "black", "band_align": "bottom",
          "title_y": 360, "title_fs": 120,
          "title_color": "&H00FFFFFF", "title_box": "&H00000000"}
```
- `bg: black` — 블러 배경 대신 순검정. 자료만 남아 화면이 정리된다.
- `title_fs: 120` — 상단 대형 120pt 2단 타이포그래피.
- `title_y: 360` — 상단 제목과 자료화면(Y=480~580) 사이의 과도한 여백을 줄이고 자연스럽게 밀착(중심 기준 Y=360).
- `hook_title` 은 **`|` 로 나눠 두 줄**로 쓴다 — 둘째 줄이 골드로 강조된다(예: "북한은 우리를 | 적으로 규정했다").
  구분자가 없으면 16자 기준으로 자동 줄바꿈. 제목 박스를 `&H00000000`(검정)으로 두면 검정 배경에 묻혀 글자만 보인다.
- `pos_y: 1420` & `anchor: 5` — 자료 하단선(Y=1280) 아래 순검정 영역에 자막을 중심 배치.
  자료의 핵심 도표·성경 구절·자막을 절대로 가리지 않는다.
★**자료는 크게 쓴다**(오너 지시: "양쪽이 조금 잘려도 되니까 시원하게 가득 채우기").
  16:9 를 그대로 넣으면 세로 523px 뿐이다. **좌우를 15% 잘라** `rect [190,0,900,600]`
  (720p 기준)으로 잡으면 1080x720 이 되어 시각적 몰입감이 극대화된다. 자르는 위치는
  화면마다 판단한다 — 텍스트가 왼쪽에 몰렸으면 `x=0`, 지도·영상은 중앙. **좌우 끝까지
  글자가 찬 화면(칠판 인용문 등)은 자르지 말고** 폭을 그대로 둔다.
- `rect` 아래끝은 원본 자막 위(720p 기준 y 600~620)에서 끊는다.
- 게이트: `check_insight.py --layout overlay`

### ✂️ 이상 컷(글리치) 안정화 기법 (2026-09-08 도입)
해설 영상은 중간에 0.3~1.5초짜리 맥락 없는 짧은 컷(AI 플래시 이미지, 도로 폭파 직전 순간 컷,
신호대 플래시, 0.5초 암전, 물음표 이모지 그래픽, 스튜디오 잔여 컷)이 섞이기 쉽다.
1. **사전 검출**: 마스터 영상(`{slug}_master.mov`)의 컷 전환점을 확인하고 튀는 컷을 식별한다.
2. **이전 화면 유지 (Hold Previous Frame)**:
   화면을 굳이 바꿀 필요가 없다면 이전의 자연스러운 관련 자료화면 프레임을 해당 구간 동안
   연장하여 마스터 영상을 정리한다 (`clean_master.mov` 생성).
3. **오디오 무손실 보존**:
   `-c:a copy`로 오디오를 그대로 유지하여 Whisper 단어 타임스탬프(`words.json`)와의 100% 동기화를 보존한다.
   클린 마스터가 준비되면 기존 마스터와 교체한 뒤 Stage B를 진행한다.

### 밴드 (1080x1920)

| 항목 | 값 | 비고 |
|---|---|---|
| 밴드 영역 | overlay `band_y=480`, `band_h=800` / 구정본 `190`, `880` | 자료가 놓이는 영역 |
| 밴드 스케일 | `min(1080/rect_w, 880/rect_h, 2.8)` | 상한 2.8배 — 720p 원본 화질 보호 |
| 밴드 테두리 | 3px `#0E1626` | 배경과 분리 |
| 배경 | 전체 프레임 9:16 확대 + `gblur=44` + 밝기 −0.28 + 채도 0.55 | 원본 제목이 읽히면 실패 |
| `fill` 모드 | 9:16 크롭 + 켄번즈 3.5% | 토킹헤드·영상 클립 전용 |

### 자막 (오너 지시 2026-09-06)

| 항목 | 값 |
|---|---|
| 언어 | **한글 단독** (영문 금지) |
| 크기 | **96pt** `Apple SD Gothic Neo, Bold` |
| 위치 | overlay `\an2\pos(540,1230)` — 자료 안 하단 / 구정본 `\an5\pos(540,1240)` |
| 배경 | **검은 박스** `BorderStyle=3`, `OutlineColour=&H1A000000`(약 90% 불투명), 패딩 14 |
| 그림자/외곽선 | **0** — 넣지 마라 |
| 줄바꿈 | 10자 초과 시 가운데 공백에서 두 줄 |
| 강조 | `hl:true` → 골드 `&H0000D7FF`, 편당 2~4개 |
| 타이밍 | 무간극 `end[i] = start[i+1]` |

`plan.json` 에 `hook_title` 을 넣으면 상단에 고정 타이틀이 뜬다. **원본 슬라이드에 이미
제목이 있으면 쓰지 마라** — 제목이 두 번 보여 지저분하다(2026-09-06 실측).

---

## ⚠️ 이 파이프라인에서 실제로 터졌던 것

| 증상 | 원인 | 대응 |
|---|---|---|
| 27분부터 같은 문장 19분 반복 | Whisper 컨텍스트 이월 환각 루프 | 긴 전사는 `-mc 0` (금기 8) |
| `.../v2/out/_work/v2/out/_work/seg_000.mov` 없음 | shorts_v2 모듈이 concat 목록에 상대경로를 쓴다 | `run_insight.py` 가 인자를 절대경로로 바꾼다 |
| `Undefined constant 'N'` | zoompan 에는 총 프레임수 상수 `N` 이 없다 | 샷 프레임수를 파이썬에서 계산해 상수로 박는다 |
| 자막이 카드 한가운데를 덮음 | 자료를 9:16 전체화면으로 채웠다 | `band` 모드로 자료를 밴드에 얹고 자막은 그 아래 |
| 밴드 뒤 배경에서 원본 제목이 읽힘 | 블러가 약함(sigma 26) | sigma 44 + 밝기 −0.28 + 채도 0.55 |
| 게이트가 BorderStyle 을 14.0 으로 읽음 | ASS Style 필드 인덱스 오프셋 | `f[15]=BorderStyle, f[17]=Shadow, f[18]=Alignment` |
| 뉴스 클립인데 토킹헤드로 판정 | 화면 속 인물 얼굴을 YuNet 이 잡는다 | `focus` 로 `band` 지정 |
| 확대했더니 자료 일부가 잘림 | 축소된 타일 이미지에서 좌표를 읽었다 | 원본 크기 프레임으로 좌표 확정 |
| 썸네일 문구가 배경 자료에 묻힘 | 본편과 같은 상단 배치로 프레임을 뽑았다 | 프레임 추출 때 `--band-y 800` |
| 우리 자막이 원본에 박힌 자막과 겹침 | 유튜브 해설 영상은 대개 자막이 하드번인돼 있다 | `rect` 높이를 620 안팎으로 잘라 **하단 100px 를 버린다**(2026-09-07) |
| 자막이 뉴스 헤드라인·성경구절을 덮음 | overlay 는 자료 하단에 자막이 얹힌다 | 청크를 **10자 이하 한 줄**로 끊고, 자료의 핵심 텍스트가 위쪽에 오도록 `rect` 를 잡는다 |
| 자료를 확대했더니 인용 출처 줄이 잘림 | 납작한 `rect` 는 `band_align=bottom` 에서 아래로 몰려 자막에 먹힌다 | 텍스트가 큰 화면은 확대하지 말고 `[0,0,1280,620]` 전체로 두는 편이 낫다 |
| 썸네일 아래에 원본 자막 잔상 | 썸네일 프레임을 블러 배경으로 뽑았다 | `extract_frames_insight.py --bg black` |
